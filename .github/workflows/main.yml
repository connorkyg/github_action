name: Windows echo Workflow
on:
  push:
    branches:
      - main

jobs:
  echo:
    runs-on: ["Windows", "self-hosted"]


    steps:
      - name: test
        shell: bash
        run: |
          mkdir sample_dir
