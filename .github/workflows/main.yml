name: Windows echo Workflow

on:
  push:
    branches:
      - main

jobs:
  please:
    runs-on: ["Windows", "self-hosted"]
    steps:
      - name: test
        run: uname > C:\Users\YK.KWON\Desktop\aaaaaaa.txt
        shell: C:\"Program Files"\Git\usr\bin\git-bash.exe -c {0}
